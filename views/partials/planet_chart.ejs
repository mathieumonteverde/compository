<section class="planet-chart-container">
  <div class="planet-chart-visual">
    <canvas id="planetChart"></canvas>
  </div>
  <div class="planet-chart-info">
    <div class="close">< Back to statistics</div>
    <div class="expand">+</div>

    <section class="instructions">
      <h3>Graphic comparison</h3>
      <p>
        Each planet represents a repository.
      </p>
      <ul class="list-group">
        <li class="list-group-item">The more recently updated the repo, the closer the planet will be to the center.</li>
        <li class="list-group-item">The more commits, the faster the rotation.</li>
        <li class="list-group-item">The more contributors, the bigger the planet.</li>
      </ul>

      <p>
        Your ideal repository is basically the biggest, fastest planet that is
        also the closest to the center...
      </p>
    </section>

    <section class="name-list">
      <h3>Repositories</h3>
      <% for (var i=0; i < data.list.length; i++) { %>
        <div class="name">
          <div class="color-badge color-badge-<%= i %>"></div>
          <h3><%=data.list[i].name%></h3>
        </div>
      <% } %>
    </section>
  </div>
</section>
